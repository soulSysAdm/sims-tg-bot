

{
  "version": 2,
  "builds": [
    { "src": "/dist/api/webhook.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/webhook", "dest": "/dist/api/webhook.ts" },
    { "src": "/check", "dest": "/dist/api/webhook.ts" }
  ]
}